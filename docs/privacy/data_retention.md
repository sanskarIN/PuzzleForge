# Data Retention

Local records remain until the player clears history, resets a category, deletes all app data, uninstalls the app, or the operating system removes storage. Deleting all local data requires confirmation and reinitializes safe defaults. User-exported files and operating-system backups are separate copies controlled by the user or platform and are not remotely deletable by PuzzleForge.
