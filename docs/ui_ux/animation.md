# Animation

Default transitions use 150–300 ms easing and communicate state changes. Solved celebrations are brief, non-blocking, and never flash rapidly. Reduced motion replaces movement with opacity or immediate state changes. The app introduces no artificial splash delay and pauses recurring animation when not visible.
